<?xml version="1.0" encoding="utf-8" ?>
<ContentPage 
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:Money.IO"
    x:Class="Money.IO.MainPage"
    Title="Money.IO">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Label
            Grid.Row="1"
            HorizontalOptions="CenterAndExpand"
            VerticalOptions="CenterAndExpand"
            FontSize="48"
            Text="{Binding Sum, Mode=TwoWay, StringFormat='{0:F2}'}"/>
        <Label
            Grid.Row="2"
            HorizontalOptions="CenterAndExpand"
            VerticalOptions="CenterAndExpand"
            FontSize="28"
            Text="{Binding Rate, Mode=TwoWay, StringFormat='{0:F4}'}"/>
        <Button
            Grid.Row="4"
            Text="Income"
            Command="{Binding IncomeCommand}"/>
        <Button
            Grid.Row="5"
            Text="Outcome"
            Command="{Binding OutcomeCommand}"/>
    </Grid>

</ContentPage>
